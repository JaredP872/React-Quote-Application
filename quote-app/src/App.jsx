// this line imports the QuoteDisplay component from the QuoteDisplay.jsx file
import QuoteDisplay from "./QuoteDisplay";

// Define the App component
const App = () => {
  // Return the QuoteDisplay component
  return (
    <div>
      <QuoteDisplay />
    </div>
  );
};

// Export the App component
export default App;
